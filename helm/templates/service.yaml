apiVersion: v1
kind: Service
metadata:
  name: {{ include "canvas-heal.fullname" . }}
  labels:
    {{- include "canvas-heal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "canvas-heal.selectorLabels" . | nindent 4 }}
